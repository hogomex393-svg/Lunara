# Lunara Demo 视频 · 拍摄脚本（2 分 40 秒）

> 这份脚本设计为"照着念照着点就行"——不需要现场发挥。
> 录之前先按"准备清单"把环境调好，然后从 Shot 1 开始顺录到 Shot 6，最后导出。

---

## 准备清单（录制前 10 分钟）

### 工具
- **屏幕录制**：推荐 **OBS Studio**（免费）或 **Windows 自带 Xbox Game Bar**（按 `Win + G`，最简单）。Mac 用户用 QuickTime 或 ScreenStudio。
- **后期裁剪/合并**（可选）：剪映 / CapCut / DaVinci Resolve（都免费）

### 环境
1. **关闭所有无关窗口和通知**：微信、QQ、邮箱、系统通知——全关或开勿扰
2. **浏览器准备 3 个 tab，事先打开**：
   - Tab 1: `http://localhost:3000`（Lunara App）
   - Tab 2: `http://localhost:3000/#/admin`（Admin 后台）
   - Tab 3: VSCode 或 GitHub 网页打开 `server/lib/llm.js` 文件
3. **后端、前端都跑起来**：
   ```bash
   # 终端 1
   cd server && npm start
   # 终端 2
   npm start
   ```
4. **预先做一次完整对话**填充 admin 后台数据：先在 AIChat 里随便聊 3-5 条，包括至少 1 条触发工具调用的（例如"过去 7 天我的情绪怎么样"），让 Call Log 和 Token Usage 都有内容
5. **浏览器缩放调到 100%**（Ctrl+0），分辨率 1920×1080 最佳
6. **录制前喝口水**清嗓子，语速放慢一点

### 录制设置（OBS）
- 视频：1920×1080，30 fps
- 音频：麦克风采样率 48kHz
- 格式：MP4
- 录制按钮一按，等 1 秒再开始说话（方便后期裁掉前面的"咔嚓"声）

---

## 分镜表（共 6 个 Shot，总长 2 分 40 秒）

每个 Shot 我都给出三栏：**时间 · 屏幕动作 · 旁白**。旁白照着念就行，每行末尾的 `[停顿]` 是给你换一口气的时间。

---

### Shot 1 · 开场（0:00 – 0:18）

**屏幕**：浏览器停在 Tab 1，Lunara App 首页（Home 屏幕，能看到 cycle ring 和今日 mood overview）

**动作**：静止 3 秒后，缓慢移动鼠标圈一下 cycle ring → 圈一下底部 tab bar

**旁白：**
> 大家好，这是 Lunara——我做的一个女性健康追踪应用。它最初是一个 React 前端 prototype，但我把它改造成了一个完整的 AI 应用案例，重点在底层这一套 AI 工程化的能力。今天用 2 分多钟带大家走一遍。[停顿]

---

### Shot 2 · 普通聊天 + 上下文注入（0:18 – 0:55）

**屏幕**：点底部 tab bar 的 "🤖 Luna AI"，进入聊天界面

**动作**：
1. 0:18 点进 AI Chat 屏幕（注意右上角"● BFF Online"绿色状态）
2. 0:23 鼠标圈一下顶部的 Default / Coach / Data 三个模板按钮
3. 0:30 在输入框打字（或点 quick prompt）："I have cramps today"
4. 0:35 等 Luna 回复
5. 0:42 鼠标指一下回复气泡下方的"🔧 get_recent_symptoms · 2640ms"小标签

**旁白：**
> 这是 Luna，应用的 AI 助手。注意右上角"BFF Online"——所有 LLM 调用都走我自己写的后端，浏览器里没有任何 API key。上面这三个按钮是不同的 Prompt 模板：默认、教练模式、数据分析模式。我先用默认模式问一个常见问题。[停顿——等 Luna 回复]
>
> 看回复底下这一行小字——"调用了 get_recent_symptoms 工具"。Luna 不是泛泛回答，而是真的去查了我最近的日志，结合我现在的周期阶段给出建议。这套"基于业务数据的 Agent 回答"是这个项目的核心。[停顿]

---

### Shot 3 · 切换模板对比效果（0:55 – 1:30）

**屏幕**：还在 AI Chat 界面

**动作**：
1. 0:55 点 "📊 Data" 模板按钮（按钮变粉色高亮）
2. 1:00 输入框打："Looking at the past two weeks, when did I log cramps?"
3. 1:05 等回复
4. 1:15 鼠标指回复内容里的具体日期 + 工具标签

**旁白：**
> 现在我切到"数据分析模式"。这个模板的 system prompt 不一样、温度调低到 0.3、并且被强制要求每次回答前先调数据工具。来问一个真正基于数据的问题。[停顿——等回复]
>
> 看，它给出了具体日期：May 5、May 12、yesterday——这是真的从我的 SQLite 数据库里读出来的，不是 LLM 编造的。同一份 Agent 代码，换个模板就换了人格——模板都是 JSON 配置，不用改代码。[停顿]

---

### Shot 4 · Admin 后台一：调用日志（1:30 – 2:00）

**屏幕**：切到 Tab 2，`http://localhost:3000/#/admin`，进入后输密码"lunara-admin-2026"，进入 Templates tab

**动作**：
1. 1:30 切到 Tab 2，看到登录页 → 0.5 秒后输完密码进入
2. 1:35 默认在 "📝 Prompt Templates" tab，鼠标圈一下左侧 4 个模板，点开 Data Analyst
3. 1:42 鼠标指右侧的 System Prompt 文本框、Temperature 输入框、Max Tokens 输入框
4. 1:50 点顶部 "📒 Call Log" tab，鼠标滑过最近几条调用记录

**旁白：**
> 这是内部管理后台——通过 /#/admin 路由进入，bearer-token 鉴权。左边是 4 个 Prompt 模板，我可以直接在这里改 system prompt、温度、最大 token，保存就生效，不用重新发版。[停顿]
>
> 切到 Call Log，每一次 LLM 调用都有完整审计：时间、模板、模型、token 用量、延迟、调用了哪些工具、成功还是失败。这就是 JD 里要求的"日志能力"——pino 写 JSONL 文件、同时入 SQLite，两份冗余。[停顿]

---

### Shot 5 · Admin 后台二：Token 用量 + 评估（2:00 – 2:25）

**屏幕**：admin 页面

**动作**：
1. 2:00 点 "📊 Token Usage" tab，能看到每天的 token 用量条形图
2. 2:08 切到 VSCode 或 GitHub 网页（Tab 3），打开 `server/evals/RESULTS.md`
3. 2:15 滚动到 TL;DR 表格，鼠标指三行变体对比

**旁白：**
> Token 用量按天聚合的视图——可以快速发现哪天哪个模板在烧钱。[停顿]
>
> 最后看一个东西：我写了一个 Prompt 评估流程，18 个测试用例分别跑在三个模板配置上，输出对比报告。基线零工具调用、加了 context 和 tools 之后 pattern detection 类问题的命中率从 0% 跳到 100%，token 成本只增加 2.2 倍——这是个可量化的工程权衡。[停顿]

---

### Shot 6 · 收尾（2:25 – 2:40）

**屏幕**：切回 README 或 GitHub 仓库主页

**动作**：滚动展示一下 README 顶部的 Mermaid 架构图，停留 3-4 秒

**旁白：**
> 整个项目的架构图、JD 对应表、案例研究 PDF 都在 GitHub 仓库的 README 里。我尽量按"如果这是个能上线的内部 AI 服务"的标准来做工程化——这是 Lunara。谢谢观看。

---

## 录制小贴士

1. **录两遍**：第一遍当作练习，第二遍是成品。差距很大。
2. **不要剪太碎**：每个 Shot 一镜到底，OK 就直接拼，不要每句话都剪——会显得断断续续。
3. **语速**：比你平时讲话慢 20%。中文听感更稳。
4. **手不要抖**：鼠标移动尽量做圆弧/直线，不要乱晃。
5. **声音 levels**：录完用耳机听一遍，麦克风音量不要爆音也不要太小。
6. **如果磕巴了**：停下来，深呼吸，从当前句重新念。后期把磕巴段裁掉就行。

## 导出 / 上传建议

- **格式**：MP4 (H.264)
- **分辨率**：1920×1080
- **大小**：控制在 50-80MB（B 站/YouTube 都没问题）
- **平台**：
  - B 站（国内 HR 容易看）→ 上传后取链接
  - YouTube（海外公司）→ 设为 Unlisted（链接可见）
  - 或者直接放进 GitHub Release，附个 mp4 文件

## 投递时如何附视频

把视频链接加到：
1. GitHub README 顶部（"## 视频演示"小节，紧跟在标题下面）
2. `PROJECT_STATEMENT.md` 简历版里那段最后加一句"演示视频：[链接]"
3. 投递邮件正文最后

---

需要我再帮你写英文版本旁白吗？或者哪个 Shot 你觉得讲得不够顺，告诉我我帮你重写。
